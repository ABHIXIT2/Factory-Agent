# Pinned to versions known to work on Python 3.13/3.14.
# If you change these, run `python -m pytest` AND `python main.py` (with .env)
# to verify the bot still starts and connects to Supabase + Telegram.

python-dotenv==1.0.0
python-telegram-bot>=22.0,<23
groq>=1.2.0,<2
openai>=1.0,<3
python-dateutil==2.8.2
tabulate==0.9.0
supabase==2.4.0
gotrue<2.5            # supabase 2.4.0 expects pre-2.5 gotrue API
httpx>=0.27,<0.29
pytz==2024.1
cachetools==5.3.3
tenacity==8.2.3
rapidfuzz>=3.0,<4    # Fast-path: customer search fuzzy matching
